<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">

        <activity android:name=".createAccount.SignUp1" />
        <activity android:name=".forgetPassword.SuccessMessage" />
        <activity android:name=".createAccount.SignUp2" />
        <activity android:name=".Login" />
        <activity android:name=".forgetPassword.PasswordReset" />
        <activity android:name=".forgetPassword.SetPassword" />
        <activity android:name=".createAccount.VerifyOtp" />
        <activity android:name=".forgetPassword.ForgetPassword" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".onBoarding.OnBoarding" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.NoActionBar"
            android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>